# 重要提示: 复制此文件为 .env.local 并填入您的实际值

# Supabase 配置
# 从 Supabase 项目设置 > API 获取这些值
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Cloudinary 配置 (用于图片上传备选方案)
# 从 Cloudinary 控制台获取这些值
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=your_cloudinary_cloud_name
NEXT_PUBLIC_CLOUDINARY_UPLOAD_PRESET=your_cloudinary_upload_preset
CLOUDINARY_API_KEY=your_cloudinary_api_key
CLOUDINARY_API_SECRET=your_cloudinary_api_secret

# ImgBB 配置 (用于图片上传备选方案)
# 从 https://api.imgbb.com/ 获取API密钥
NEXT_PUBLIC_IMGBB_API_KEY=your_imgbb_api_key

# 应用配置
NEXT_PUBLIC_APP_URL=http://localhost:3000

# 注意:
# 1. 确保在 Supabase 中创建了名为 "artworks" 和 "avatars" 的存储桶
# 2. 确保存储桶设置为公开访问
# 3. 如果您遇到 "Bucket not found" 错误，请检查您的 Supabase 项目中是否存在这些存储桶
# 4. 如果 Supabase 存储上传失败，系统将自动尝试使用 ImgBB 作为备选方案

# NextAuth 配置（可选，后续扩展用）
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your_nextauth_secret 